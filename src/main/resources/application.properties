# Database (기존 유지)
spring.datasource.url=jdbc:mariadb://175.114.229.152:3306/ai_agent?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=5whakfldkelql

# CSV Import 설정 (기존 유지)
app.csv.base-dir=./data
app.csv.encoding=UTF-8
app.loader.create-id=true
app.loader.truncate=false
app.import.on-start=true

# JPA 설정 (추가)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# Logging (추가)
logging.level.root=INFO
logging.level.com.AIagnet.agent=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Jackson (추가)
spring.jackson.time-zone=Asia/Seoul
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.default-property-inclusion=non_null

# Swagger UI 설정
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.doc-expansion=none
springdoc.swagger-ui.default-models-expand-depth=1
springdoc.swagger-ui.default-model-expand-depth=1
springdoc.packages-to-scan=com.AIagnet.agent
springdoc.paths-to-match=/api/**

# FastAPI 서버 설정
ai.fastapi.url=http://localhost:8000
ai.fastapi.timeout.connect=5000
ai.fastapi.timeout.read=30000